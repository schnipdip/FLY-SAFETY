# FLY-SAFETY
Checks METAR data and gives an estimated return of 'weather' it's safe to fly. 

Requirements: 
- ngrok.exe
- Twilio:
  - pip install twilio
  - Twilio phone number
- pip install emoji
- pip install flask
- pip install bs4
